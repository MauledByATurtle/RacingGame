from pygame import Color  # Only needs colors

colors = {'black' : (0,0,0),  # This is all the colors
          'white' : (255,255,255),
          'gray' : (128,128,128),
          'red' : (255,0,0),
          'blue' : (0,0,255),
          'green' : (0,255,0)}

screen_size = (1280, 720)